import LayoutProps from '../../../types/layout'

const Button: React.FC<LayoutProps> = ({children}) => {
    return(
        <input type="button" placeholder='' onClick={} />
    )
}

export default Button
